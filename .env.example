# Frontend (Next.js) environment — copy to .env.local for development.
# Base URL of the Laravel API, including the /api suffix.

# Local development (Laravel `php artisan serve`):
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Production:
# NEXT_PUBLIC_API_URL=https://api3.a7mdgmal.online/api
